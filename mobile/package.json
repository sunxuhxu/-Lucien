{
  "name": "xumo-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor Android wrapper for the Xumo web application",
  "scripts": {
    "cap": "node --require ./scripts/node-os-workaround.cjs ./node_modules/@capacitor/cli/bin/capacitor",
    "sync": "npm run cap -- sync android",
    "open": "npm run cap -- open android",
    "build:debug": "npm run sync && cd android && gradlew.bat assembleDebug",
    "build:release": "npm run sync && cd android && gradlew.bat bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "typescript": "^5.7.0"
  }
}
